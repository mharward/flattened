name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: Runs unit tests
      run: npm test
    - name: Runs npm build
      run: npm run build
